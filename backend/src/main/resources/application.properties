# ===== JWT =====
jwt.secret=E+6e1i+WLSVlmM20WziYYVFkCVdc173TCKSduirt2KQ=

# ===== MySQL =====
spring.datasource.url=jdbc:mysql://localhost:3306/coconutoilsystemdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=chamo123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===== JPA / Hibernate =====
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# ===== CORS =====
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# --- Mail (use your provider / app password) ---
# Gmail SMTP (TLS)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=email
spring.mail.password=password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# use the same address as the sender to avoid rewrites
spring.mail.from=chamodikumarage2003@gmail.com

# uploads root (relative or absolute)
app.upload-dir=uploads

# multipart limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ===== DEBUG LOGGING (temporary) =====
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web.cors=DEBUG
logging.level.org.springframework.web.filter.CorsFilter=DEBUG
logging.level.com.backendproject.backend=DEBUG
